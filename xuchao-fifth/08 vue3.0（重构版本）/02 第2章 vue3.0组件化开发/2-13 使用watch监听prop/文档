本例子的示例见ButtonSingleWatch组件

在ButtonSingleNorm中，从父组件传入的props数据，会被子组件的一个中间变量保存
但是这种保存不是响应式的，也就是说，如果props数据在父组件内部发生改变
在子组件的中间变量不会即时响应

那么可以在子组件中把props数据放在watch里进行监听
当props发生变化的时候，中间变量也响应变化

以保证单项数据流全程都是响应式的