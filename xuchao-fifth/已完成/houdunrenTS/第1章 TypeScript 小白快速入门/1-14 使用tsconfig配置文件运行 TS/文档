1 创建配置文件 在当前文件夹终端下运行 tsc --init
  出现tsconfig.json文件

2 仍然是在当前文件夹终端下运行 tsc -w
  则在此文件夹下创建ts文件会自动生成同名的js文件
  
  这样做的好处是在这个文件夹之下
  不用每次创建ts文件之后都要运行tsc index.ts -w去生成js文件
  而是会自动生成

往后如果需要对一些配置进行修改，比如修改any类型，就可以在这个配置文件下运行
