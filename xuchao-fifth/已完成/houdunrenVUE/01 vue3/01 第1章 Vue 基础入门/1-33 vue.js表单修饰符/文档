本例子的示例见1-18的InputFixComponent组件

现在有一个场景，需要用监听器watch监听一个响应式数据
当这个数据发送变化的时候发送请求

如果这个数据是双向绑定在一个input表单里的
那么表单里的内容每改变一次就会发送一次请求，造成巨大的性能开销

可以在v-model后面使用lazy修饰符

一般双向绑定中的数据都是字符串格式的，如果需要里面数据变成数值
可以用number修饰符

还有trim清除左右两边的空白
清理单边空白还有ltrim和rtrim