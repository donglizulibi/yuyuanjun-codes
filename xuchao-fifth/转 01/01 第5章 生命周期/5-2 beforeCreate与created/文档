本例子的示例见beforeCreate组件

一个vue组件从创建到注销有很多个生命周期
在生命周期的各个阶段的时间点称为生命周期的钩子

beforeCreate和created钩子对应于整个组件的对象实例创建前后
则对象中的data属性也会在实例创建之后才有效

但是created是在数据挂在在模板之前，所以如果在这里面打印
html中的元素对象的话，是打印不出来了

一般发送请求是在created里面