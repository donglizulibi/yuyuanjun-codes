本例的实例见DynamicComponentIsGlobal组件

如果是使用频率比较高的组件，可以在main.js里面注册一个全局组件

在这里需要定义一个根组件和全局组件的注册规范问题

在本例中，根组件App是直接放在createApp中注册的，注册根组件也就是创建实例出来
而全局组件需要依赖于创建出来的实例，也就是在根组件注册之后再注册

具体的方法就是在根组件实例的上使用component方法，这个方法有两个参数
第一个参数放全局组件的名称，也就是将来在各个组件当中挂载的标签名
第二个参数是从外部引入进来的单文件组件

当然最后一步就是mount挂载到app的div上