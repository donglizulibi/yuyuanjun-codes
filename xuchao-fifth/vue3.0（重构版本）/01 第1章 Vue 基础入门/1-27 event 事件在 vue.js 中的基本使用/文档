本例的示例见1-18的EventComponent

本例的第一次介绍见1-12

一个点击事件可以执行多个方法，在执行多个方法的时候
每个方法不管需不需要传递参数，都要加上()，且方法之间要用逗号隔开

如果点击事件只是执行一个方法，且不需要传递参数，可以不加()
要传参的话还是要加()

如果点击事件不传递参数，在vue的html里面不加()
则在methods里的方法可以加上event参数，则这个参数就表示事件对象