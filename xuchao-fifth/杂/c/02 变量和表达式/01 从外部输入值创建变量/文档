在C语言程序中输入中文，无法成功编译，或者编译成功后，运行出现乱码
原因是C语言调用win的cmd默认编码为GBK
但是vscode默认编码为utf-8

解决办法
一 修改vscode底部编码的选项，变成gbk
二 在setting中修改
三 不使用中文

设置完之后，要把原来的exe删掉，重新创建一个
否则会报错xxx.exe:permission denied

另外,如果运行程序之后,输入一个字符串而不是数字
程序会认为什么都没有输入,仍然用price的初始值参与下面的运算