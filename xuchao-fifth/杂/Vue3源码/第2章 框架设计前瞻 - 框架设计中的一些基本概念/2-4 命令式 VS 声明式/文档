评价一种编程范式优劣，主要看两个方面
1 性能  2 可维护性

首先是性能，可以从耗时量来观察
还是从渲染文本来看
假如命令式渲染文本的代码为 div.innerText = 'str'，其耗时为1个单位
而声明式渲染文本的代码为<div>{{msg}}</div>, 其耗时为n+1个单位
其中n是解析框架耗费的时间

则可以理解为命令式的性能永远大于等于声明式的性能

其次是可维护性，主要是代码可以方便阅读和增删改查
Vue的代码会比原生js更加容易阅读
所以命令式的可维护性小于声明式的可维护性