本例的示例为默认插槽的基本使用

所谓默认插槽就就是组件中只有一个固定的位置需要插入后续内容
那这个位置可以用一个slot标签来占位

后面在使用这个组件的时候，组件在根组件要以双标签的形式显示
在双标签中间放入要插入的内容
这个内容可以是文本，标签或者另一个组件

如果slot标签在组件里重复出现且不做任何处理
则插入的内容会在所有出现的位置都展示一遍

插槽中插入
1 纯文本
2 html标签
3 组件标签